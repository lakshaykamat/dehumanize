"""Subcommand implementations. Each module exposes:

    add_parser(sub)              register the argparse subparser
    run(args) -> int             execute the parsed args
    interactive() -> int         interactive wizard for this command
"""
